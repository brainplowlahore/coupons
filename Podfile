# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AlamoFireGetData' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Alamofire', '~> 3.0'
   pod 'SwiftyJSON'

  # Pods for AlamoFireGetData

  target 'AlamoFireGetDataTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
